package com.hana.orchestrator.domain.dto

import com.hana.orchestrator.domain.entity.ExecutionResult

/**
 * Domain DTO - ExecutionResult 관련 비즈니스 로직용
 * 현재는 Entity를 그대로 사용하지만, 나중에 확장 가능
 */
typealias ExecutionResultDto = ExecutionResult
